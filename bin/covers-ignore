#!/usr/bin/env php
<?php

if ($argc !== 2) {
    print "Usage: covers-ignore /path/to/test" . PHP_EOL;
    return 1;
}

$autoload = \getEnv('COMPOSER_RUNTIME_BIN_DIR');
require $autoload;

echo \TRegx\Ignore\ignoredCovers(file_get_contents($argv[1]));
